

laravel introduce the concept of Mailable, then, insead of defining the mail message in function closure, we should create a specific class to represent each mail.

<!-- `For this` -->

```bash
# DemoNewUserMailable
# An autogenerated mailable PHP class is generated, here is what it looks like
php artisan make:mail DemoNewUserMailable 
```

Checkout the `UserController


Create a MailService class and instanciate from any controller

```bash
php artisan make:service MailService
```
